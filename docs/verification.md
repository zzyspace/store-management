# 本地验收记录

日期：2026-09-09。此记录仅描述实施阶段的本地代码、临时数据库和浏览器验证；生产发布结果另行核验。

## 已通过

| 项目 | 验证 | 通过数量 |
| --- | --- | ---: |
| store-management | 字段校验、权限规则、SQLite 唯一约束、分页、核销及持久化 | 3 |
| store-management | 真实网关联调：身份、Origin、越权、并发发放核销、会话版本、网关失败 | 1 |
| admin-auth-gateway | 完整单元和接口回归，包含旧授权表迁移、数据保留及重复启动 | 62 |
| employee-information | 原有接口与权限回归 | 17 |
| invoice-submit | 原有接口与权限回归 | 22 |
| wechat-claw | TypeScript 构建及后台接口、授权测试 | 12 |
| server-infra | 路由、鉴权、发布与回滚配置测试 | 13 |
| admin-auth-gateway | 原三个后台真实统一登录与数据权限联调 | 10 |

上述自动化报告累计 140 项通过，无失败。

Chrome 浏览器验收覆盖：

- 门店管理桌面、390px 和 320px 手机宽度，浅深主题及新增弹窗；表格限制在容器内滚动，页面没有横向溢出。
- 默认账号显示名、上海时间（浏览器故意设置为洛杉矶时区）、必填赠送原因、空白原因拦截、修改操作人、发放与核销。
- 店长固定获授权门店、合伙人只读权限、账号管理的角色范围及权限表单、真实登录返回 `/store`。
- 延迟旧门店响应，在新门店完成加载后释放，确认不会覆盖当前门店列表。
- 原三个后台中“门店管理”导航的授权显示，未授权账号隐藏该入口。

`outputs/browser-check/` 包含 15 张截图与 `measurements.json`；`outputs/other-centers/` 包含原后台及导航回归截图。这些文件被 Git 忽略，画面中的账号和优惠券均为测试数据。

## 验证边界

- 本机未安装 Nginx，因此仅完成共享入口配置测试和发布脚本语法检查，未运行实际 `nginx -t`；正式发布由 server-infra 对候选配置先执行此检查，再切换入口并冒烟。
- 浏览器验证使用本机 Chrome；没有宣称已通过真实 iPhone Safari 验收。
- 未访问或修改生产账号、数据库、服务或 Nginx，未授予任何真实账号新的门店权限。

## 2026-09-14 连续扫码与批量发放

本次改动限于 store-management。本地使用临时数据库、统一网关测试账号和 Chrome 模拟摄像头，未操作生产环境。

- `npm test`：7项通过，覆盖券码解析、三店两类映射、前导零、公共字段及50张上限、部分成功、历史接口兼容、批次结果持久化重放、数据库错误事务回滚。
- `npm run test:integration`：2项通过，覆盖原有生命周期、批量授权/Origin/门店范围、并发同请求重放、同ID不同内容冲突和会话失效；本地资源包含实际 jsQR 库。
- `npm run test:browser`：通过，生成28张截图及尺寸记录。使用固定二维码图案绘制真实图片，通过 canvas MediaStream 送入实际 jsQR 解码；仅替换摄像头来源。
- 连续扫码检查证实：确认期间解码调用计数不增加，视频时间继续前进且同一视频轨道保持 live；连续确认多张只调用一次 getUserMedia，结束后轨道全部 ended。
- 覆盖取消后同一码不立即重弹、移出再扫、重复和跨店券拦截、类型自动识别、券码只读及移除/追加、原因和时间保留、部分失败清单及后续新批次。
- 模拟服务端已提交但响应丢失，再次提交的请求内容和 requestId 完全相同，返回原有成功/失败结果且券记录不重复；结果不明时表单锁定。
- 覆盖相机权限拒绝、设备中断、关闭页面后的迟到授权、退出页面和观察到401时释放相机；关闭发放弹窗清空草稿。
- 检查320px、390px及桌面浅深主题。发放弹窗内容区独立滚动，标题和发放按钮保留在可视区域。已目视检查扫码确认、券码清单及部分失败截图。

新增批次表采用幂等建表，不重写历史券表；旧代码可忽略该表，回退代码时应保留券数据库及批次记录。共享网关和 Nginx 配置未修改；单次最多50张合法券码及最大公共字段的JSON仍小于16KB。

验收边界：未连接真实 iPhone 或 Android 手机，Safari/Chrome 真机的后置镜头选择、权限对话框及现场识别效果仍需真机验收；自动化结果不代表已完成真机检查。未提交、推送或部署。

## 2026-09-14 发放弹窗溢出与长列表修正

- 移除内容区的负水平外边距，显式约束表单和内容区宽度，只保留纵向滚动；已扫描券码列表取消240px限高和独立滚动，完整展开并随表单内容滚动。
- 发放时间补充最大宽度和最小宽度约束；iOS WebKit 专用规则移除原生日期控件的水平内边距，将间距放入值区域。相关问题见 [WebKit 301648](https://bugs.webkit.org/show_bug.cgi?id=301648)。
- 原 `npm run test:browser` 回归通过（28张截图），新增弹窗/内容区/时间控件横向溢出及券码列表展开断言。
- 另在 Chromium 和 WebKit 26.5 各检查10种布局组合：320、375、390、768、1440px × 浅深主题。使用50张券码，包含200字符最长券码；内容区不能横向滚动，券码列表高度完全展开，表单可纵向滚动至最后的时间控件并修改时间。
- 实测记录和截图位于 `outputs/issue-layout-check/`。本机 WebKit 未启用 iOS 专用 CSS 特性，这些测试不代表 iPhone 原生时间选择器真机验收；iOS 兼容处理依据上述 WebKit 缺陷及现有同类页面实现。

## 2026-09-14 批量扫码核销

- 新增顶部核销入口，移除行内单张核销按钮；主列表分别展示发放操作人和核销操作人。
- 发放与核销共用扫码和表单控制逻辑，各自持有独立草稿、待重试请求和忙状态。扫码确认只加入清单，最终提交后才核销；部分成功只保留失败券及原因。
- `npm test`：14项通过，覆盖核销公共字段、时间上下界、逐券失败、50张批量、持久化重放、数据库回滚、兼容旧记录的幂等迁移及原有发放/部署检查。部署脚本新增核销接口保护检查后，其2项测试另行复验通过。
- `npm run test:integration`：3项通过，新增核销权限、跨店、同源、并发不同批次、同批次重放、独立操作人及旧单张接口兼容校验。
- `npm run test:browser`：通过，输出35张截图。覆盖扫码期间相机保持开启、扫码不提前核销、未来时间整批拒绝、空白操作人拦截、已核销/未发放/早于发放时间的逐券失败、丢失响应后原请求重放、只重试剩余券、主列表两次操作人及两类草稿隔离。
- 浏览器验证了只有发放权限、只有核销权限、只读账号的入口可见性；仅核销权限账号可进入扫码。
- Chromium和WebKit 26.5的核销弹窗长列表检查共20组通过（320/375/390/768/1440px × 浅深主题）：50张券码完整展开，含200字符券码，无横向滚动，时间可填写。记录和截图位于 `outputs/redeem-layout-check/`。
- 已目视检查手机部分失败清单、WebKit时间输入和主界面双按钮。iPhone原生相机与时间选择器未真机验收，本机WebKit未启用iOS专用CSS特性，不将自动化结果描述为真机通过。
- 数据迁移仅增加可空的 `redeemed_operator` 字段和独立核销批次表，历史发放信息与核销账号保持原值，历史缺失核销姓名显示“—”。旧版本可忽略新增字段和表；回滚代码时保留业务数据库和备份。

## 2026-09-14 已发放列表 B 版实施

- 按用户选定的独立B版预览实施紧凑列表，主列表展示券码、类型、状态、原因摘要和发放摘要；点击条目显示完整详情，手机为底部面板、桌面为侧边面板。
- 详情显示当前门店、完整券码、完整原因及两次操作人/时间，支持复制券码；历史缺失核销姓名仍显示“—”，未核销券显示暂无核销记录。查看和复制不发送业务写请求。
- 使用原生dialog并补充首尾焦点循环、Escape/遮罩关闭、关闭后焦点返回；列表刷新、切换门店或会话失效时关闭并清空旧详情。
- 延续原接口及每页50条分页；200字符券码和操作人、长原因在摘要中适当截断，详情完整显示并可纵向滚动。所有数据通过textContent写入，测试中的HTML片段保持纯文本。
- `npm run test:browser` 完整回归通过（47张截图），包括原扫码发放/核销、不同权限、分页、空列表、错误重试、复制成功/失败、键盘操作、长内容和会话失效清理。
- WebKit复验同一列表/详情检查通过（12张截图）；另保存了Chromium/WebKit、手机/桌面和浅深主题的16张正常内容实现截图，位于 `outputs/list-b-implementation/`。未连接iPhone真机。
- 本轮只修改前端列表、详情及相关验证/说明，未修改接口或数据库。没有提交、推送或部署。

## 2026-09-15 发放弹窗 B 版两步流程

- 仅发放弹窗改为“扫描券码 → 填写并确认”。第一步不验证隐藏字段，也不发送发放请求；第二步先展示公共填写项，再完整展示本批券码，最终按钮带发放数量。
- 上一步/下一步、两步中的追加扫码均保留原因、操作人和时间；移除最后一张后停留当前步骤并禁用提交，关闭后重新打开从第一步空草稿开始。
- 未确认网络结果时，上一步及表单/券码编辑均锁定，原批次重试沿用既有机制。部分失败保留第二步，只显示剩余失败券。
- `npm run test:browser` 完整回归通过（59张截图），新增两步无提前发放、字段保留、追加扫码、空清单禁用和步骤复位；原批量核销、B版列表/详情、权限及失败重试回归保持通过。
- WebKit两步流程和长清单检查通过（20张截图）；额外20种表单布局组合检查通过。长清单布局压力检查使用50条真实行结构，模拟视频流程使用实际二维码图像，均为临时测试数据。
- WebKit最初未使用相机替身，返回权限错误；已将测试替身改为覆盖navigator.mediaDevices属性并增加替身安装检查。复验时模拟视频正常输出640×640帧，完整两步扫码测试通过。业务相机实现未修改，未进行iPhone真机验收。
- 手机底部弹窗、桌面居中弹窗沿用当前浅深主题和Safari时间控件兼容规则，清单不折叠、无独立滚动区，底部操作固定。
- 本轮为本地实施，未修改接口/数据库/核销流程，未提交、推送或部署。

## 2026-09-15 扫码页底部确认卡片

- 按用户最终选择仅保留底部确认卡片，移除中央框对比样式；发放和核销共用界面，明确显示当前用途、门店、已确认数量及相机状态。
- 确认卡片位于实时相机区域下方，包含类型、门店、完整券码和加入清单提示。确认期间隐藏结束按钮并禁用顶部返回，确认/取消后恢复；未改变相机与识别引擎。
- 200字符券码可在卡片内容区纵向查看，取消/确认按钮始终可见；新券显示时重置卡片滚动位置。相机失败保留清单并提供重试，顶部返回可终止未完成的启动。
- `npm run test:browser` 完整回归通过（60张截图），覆盖底部卡片几何位置、实时轨道保持、确认时暂停解码、长二维码、权限失败、顶部返回、原发放/核销/列表流程。
- Chromium和WebKit独立连续扫码检查分别通过（各10张截图）。途中出现追加扫码等待超时，已补充失败观测信息；新测试会话先清空上一张模拟画面，最终完整回归通过。
- 已目视检查浅色确认卡片、深色扫描状态和320px长券码；本机使用模拟视频与真实二维码图像，未进行iPhone真机验收。
- 本轮为本地修改，未提交、推送或部署。

## 2026-09-15 优惠券术语统一为激活

- 当前界面及动态提示中的“发放”统一替换为“激活”：顶部入口、已激活列表、详情记录、两步弹窗、时间标签、扫码用途/确认提示、重试和错误信息均已覆盖。
- admin-auth-gateway 的 `coupon:issue` 权限显示名同步改为“激活优惠券”。权限键、接口路径、数据字段和历史业务数据不变；旧验收记录和独立设计稿保留原貌。
- 门店管理14项单元测试、账号管理2项门店权限测试、完整浏览器回归（60张截图）通过。当前运行代码中已无旧中文术语。
- 此次仅完成本地修改，包含此前尚未提交的底部扫码确认卡片，未提交、推送或部署。

## 2026-09-15 核销界面 A 版实施

- 按用户选择实施单页核销：扫码清单在上，操作人和核销时间在下；手机底部弹窗、桌面居中弹窗，底部操作固定。激活仍保留两步流程。
- 空清单显示大扫码入口并禁用核销；扫码后显示继续扫码和完整清单，按钮带本批数量；错误提示位于清单上方，失败原因逐券展示，明确区分已核销、未激活与时间不符。
- 保留所有核销业务校验及批次重试机制；扫码前填写的操作人和时间返回后保留，移除最后一张后恢复空清单状态。
- `npm run test:browser` 完整回归通过（60张截图），覆盖A版新增交互、部分失败与重试，以及原激活两步流程、扫码和列表详情。
- WebKit核销流程与长列表检查通过（17张截图），含已确认结果输入、时间校验和不同宽度下50行的布局压力检查；已目视检查手机普通清单和部分失败。未进行iPhone真机验收。
- 本次未改接口或数据库结构，部署沿用固定提交及数据库备份/回滚流程。
